// here I add an array with 10 values, adding names to each username.//
let username = ['Nat','Mat','Pam','James','Nick','Pat','Sandra','Colleen','Kevin','Jada'];
let  user_age = [18,19,20,21,25,30,35,37,40,41];

// I have add each user a age that is aleast 18 years and older by writting a while statmente//

let username_count = 0;
while(username_count > username.length) {
    console.log(username = [username_count]);
    console.log(user_age [username_count]);
    console.log(user_age = username_count ['username'])
    username_count ++;
}

 
//this is for statement to add each user_age to each username
for(username_count = 0; username_count < username.length; username_count + 1);